default_view:
  name: DEFAULT
  view: yes
  icon: mdi:home
  entities:
    - group.mobile_devices
    - group.security
    - group.climate
    - group.safety
    - group.switch_control
    - device_tracker.mwood_nexus6p
    - device_tracker.kwood_iphone
    - alarm_control_panel.ha_alarm
    - updater.updater
    - camera.foscam_camera

battery_view:
  name: Battery
  view: yes
  icon: mdi:battery
  entities:
    - group.power

status_view:
  name: Status
  view: yes
  icon: mdi:chip
  entities:
    - sensor.speedtest_download
    - sensor.speedtest_ping
    - sensor.speedtest_upload
    - group.sensor_pi

debug_view:
  name: Debugging
  view: yes
  icon: mdi:database
  entities:
    - group.automation_group
